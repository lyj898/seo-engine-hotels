---
import siteConfig from '../lib/config.js';
import { urls } from '../lib/urls.js';
import Logo from './Logo.astro';
import ContactButton from './ContactButton.astro';
import ThemeToggle from './ThemeToggle.astro';

// One nav definition, rendered twice (desktop inline row, mobile scroll
// row) so the two can never drift apart.
const navLinks = [
  { href: urls.categoriesIndex(), label: siteConfig.categoryLabelPlural ?? `${siteConfig.categoryLabel}s` },
  { href: urls.regionsIndex(), label: siteConfig.regionLabelPlural ?? `${siteConfig.regionLabel}s` },
  ...(siteConfig.enabledFeatures?.listicles ? [{ href: urls.listiclesIndex(), label: 'Recommends' }] : []),
  { href: urls.about(), label: 'About' },
];
---

<header
  class="sticky top-0 z-40 border-b border-brand-divider bg-brand-bg/95 backdrop-blur-sm dark:border-brand-divider-dark dark:bg-brand-bg-dark/95"
>
  <div class="mx-auto flex max-w-6xl items-center justify-between gap-3 px-4 py-4 sm:px-6 sm:py-5">
    <a
      href={urls.home()}
      class="flex shrink-0 items-center gap-2.5 font-serif text-lg font-normal leading-none tracking-tight"
    >
      <Logo size={26} class="shrink-0" />
      {siteConfig.siteName}
    </a>

    <nav class="kicker hidden items-center gap-8 text-brand-muted dark:text-brand-muted-dark md:flex" aria-label="Primary">
      {navLinks.map((link) => (
        <a href={link.href} class="hover:text-brand-primary dark:hover:text-brand-primary-hover-dark">{link.label}</a>
      ))}
    </nav>

    <div class="flex shrink-0 items-center gap-2">
      <ContactButton />
      <ThemeToggle />
    </div>
  </div>

  <!--
    Mobile nav. The desktop row is `hidden md:flex`, which previously left
    phones with no way to reach Categories / Regions / About at
    all. This second row carries the same links below the md breakpoint as a
    horizontally scrollable strip -- no hamburger, no JS, and the links stay
    in the markup for crawlers on every page.
  -->
  <nav
    class="-mt-px flex gap-4 overflow-x-auto border-t border-brand-divider px-4 py-2.5 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden dark:border-brand-divider-dark md:hidden"
    aria-label="Primary (mobile)"
  >
    {navLinks.map((link) => (
      <a
        href={link.href}
        class="kicker shrink-0 whitespace-nowrap text-brand-muted transition-colors hover:text-brand-primary dark:text-brand-muted-dark dark:hover:text-brand-primary-hover-dark"
      >
        {link.label}
      </a>
    ))}
  </nav>
</header>
